import React from 'react'

const MovieList = ({ title, movies }) => {
    console.log(movies)
    return (
        <div>
            <div>
                <h1>{title}</h1>
                <div>

                </div>
            </div>
        </div>
    )
}

export default MovieList